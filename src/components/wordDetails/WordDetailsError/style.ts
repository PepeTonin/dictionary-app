import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    paddingHorizontal: 16,
    paddingTop: 16,
  },
  wordContainer: {
    flex: 1,
    gap: 8,
    alignItems: "center",
  },
  wordText: {
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
  },
  regularText: {
    fontSize: 16,
    textAlign: "center",
  },
});
